name: Deploy to QA
on:
  workflow_dispatch:
jobs:
  install:
    name: Install dependencies
    runs-on: ubuntu-latest
    outputs:
      cache-hit: ${{ steps.cache.outputs.cache-hit }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/cache@v4
        id: cache
        with:
          path: ~/.npm
          key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-node-
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"
      - name: Install dependencies
        run: npm ci
  deploy-api:
    name: Deploy API
    runs-on: ubuntu-latest
    environment:
      name: QA
    needs: install
    steps:
      - name: Deploy api to QA environment
        run: |
          echo "Deploying API to production..."
          # Add your deployment commands here, e.g., using SSH or a cloud provider CLI
  deploy-web:
    name: Deploy Web
    runs-on: ubuntu-latest
    environment:
      name: QA
    needs: install
    steps:
      - name: Deploy web to QA environment
        run: |
          echo "Deploying web application to production..."
          # Add your deployment commands here, e.g., using SSH or a cloud provider CLI
